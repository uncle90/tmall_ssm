<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.finstone.tmall.mapper.CategoryMapper">
    <!--<select id="total" resultType="int">
        select count(*) from category
    </select>-->

    <select id="list" resultType="Category">
        select * from category order by id desc
        <!--<if test="start!=null and count!=null">
            limit #{start},#{count}
        </if>-->
    </select>

    <!-- keyProperty 指定哪个属性用作POJO的主键-->
    <insert id="add" parameterType="Category" useGeneratedKeys="true" keyProperty="id">
        insert into category(name) values (#{name});
    </insert>

    <delete id="delete" parameterType="int">
        delete from category where id = #{id};
    </delete>

    <update id="edit" parameterType="Category">
        update category set name = #{name} where id = #{id};
    </update>

    <select id="get" parameterType="int" resultType="Category">
        select * from category where id = #{id};
    </select>

</mapper>